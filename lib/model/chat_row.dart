

class ChatRow {
  String name;
  String lastMessage;
  int unreadMessages;
  String timestamp;
  String profileUrl;

  ChatRow({this.name, this.lastMessage, this.unreadMessages, this.timestamp, this.profileUrl});

}